service: sls-starwars

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: us-east-1

functions:
  searchPlanet:
    handler: handler.searchPlanet
    events:
      - http:
          method: get
          path: /planets/{name}
